const sections = document.querySelectorAll('section');


function createNavItem() {
    const aLink = document.createElement('a');
    const liLink = document.createElement('li');
    const linkTitle = document.                                                                                             
}

for (let i = 0; i <= sections.length; i++) {
    createNavItem();
}